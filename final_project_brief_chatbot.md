# פרויקט סיום — צ'אטבוט אימות ותורים על בסיס ה-API הקיים שלכם

## רקע

יש לכם כבר API מלא לכל Entity מפרויקט האמצע (לקוחות, תורים וכו'). בפרויקט הסיום לא בונים משהו חדש מאפס — **בונים שכבת שיחה טבעית מעל מה שכבר קיים**. המטרה: משתמש כותב משפט חופשי, והבוט יודע לזהות אותו, לאמת אותו, ולתת לו את המידע האמיתי מהמערכת — גם אם הוא עצמו טעה.

## דוגמת שיחה (התרחיש המרכזי שהפרויקט צריך לתמוך בו)

```
משתמש: קוראים לי רותם ויש לי תור בתאריך 18.01.2027
בוט:    קוראים לך רותם מירון?
משתמש: כן
בוט:    מה תעודת הזהות שלך? (לצורך אימות בלבד)
משתמש: 123456789
בוט:    [מאמת מול ה-API — מספר תואם]
        מצאתי אותך! שימי לב: התור שלך בפועל הוא ב-19.01.2027 בשעה 19:00
        (ולא ב-18.01 כפי שציינת).
```

זו לא רק "הצגת מידע" — זה **זיהוי, אימות, ותיקון**. הבוט לא סתם עונה על שאלה, הוא מגלה שהמשתמש טעה ומתקן אותו על סמך הדאטה האמיתי.

## ארכיטקטורה מוצעת (4 שכבות)

**1. הבנת שפה טבעית (NLU)** — Gemini מקבל את המשפט החופשי ומחזיר JSON מובנה, למשל:
```json
{"name": "רותם", "claimed_date": "2027-01-18"}
```
זה לא RAG — זו חילוץ מידע (extraction). תנו ל-Gemini פרומפט קשיח שמכריח אותו להחזיר רק JSON בפורמט קבוע, כדי שתוכלו לפרסר אותו בקוד באופן אמין.

**2. חיפוש לקוח** — קריאה ל-API הקיים שלכם לפי שם (חלקי). אם יש כמה תוצאות תואמות (כמה "רותם")— **אל תנחשו**, תשאלו הבהרה עם שם מלא, בדיוק כמו בדוגמה.

**3. אימות (Identity Verification)** — לפני שחושפים כל מידע, מבקשים תעודת זהות ומשווים בקוד מול הערך שחוזר מה-API עבור אותו לקוח. רק התאמה מדויקת מאפשרת המשך.

**4. תשובה מבוססת דאטה אמיתי** — אחרי אימות, שולפים את התור האמיתי מה-API, משווים לתאריך שהמשתמש טען, ומנסחים תשובה טבעית עם Gemini שמדגישה את הפער אם יש כזה.

## דרישות פונקציונליות

- חיפוש לקוח לפי שם חלקי, כולל טיפול במקרה של כמה התאמות (הבהרה, לא ניחוש).
- אימות תעודת זהות אמיתי מול הדאטה — לא רק "בדיקה שהוזן משהו".
- **לפני אימות מוצלח — אסור לחשוף שום פרט אישי** (לא תאריך, לא שעה, לא כלום). זו לא הערת אגב, זו דרישה קשיחה.
- הגבלת מספר ניסיונות אימות (למשל 3), ואז חסימה מנומסת של השיחה.
- טיפול הגיוני במקרים החריגים: שם שלא קיים, לקוח בלי אף תור, מספר זהות שגוי.
- ניהול מצב שיחה — הבוט צריך לזכור בין הודעה להודעה מי זה "המועמד" הנוכחי ואם הוא כבר אומת, לא רק לענות על כל הודעה בבידוד.

## תרחישי בדיקה שחובה להריץ בפועל

1. שם ייחודי + תאריך שגוי שהמשתמש טוען → תיקון נכון עם התאריך והשעה האמיתיים.
2. שם לא ייחודי (2+ לקוחות עם אותו שם פרטי) → הבוט שואל ומבהיר, לא מנחש.
3. תעודת זהות שגויה → סירוב מנומס, בלי לחשוף שום פרט על התור.
4. לקוח קיים בלי אף תור פתוח.
5. שם שלא קיים בכלל במערכת.

## דרך אגב — Web + DevOps מינימלי

הצ'אטבוט צריך לרוץ כאפליקציית **web** (למשל Flask/FastAPI עם ממשק צ'אט פשוט, או Streamlit) — לא רק כתא בקולאב. הפריסה לא צריכה להיות אוטומטית (בלי CI/CD מלא), אבל היא כן צריכה נוהל **DevOps מסודר**: כשיש עדכון קוד, מריצים תהליך ברור של משיכה ידנית מ-GitHub (`git pull`) לשרת שבו האפליקציה רצה, ומפעילים מחדש. תעדו את זה כתהליך חוזר ועקבי — לא "עשיתי את זה פעם אחת ידנית ושכחתי איך".

## הגשה

- קישור לריפו GitHub עם קוד האפליקציה (כולל שכבת ה-NLU, שכבת ה-API calls, ולוגיקת האימות).
- קובץ `DEPLOY.md` שמתאר בצעדים ברורים: איך מריצים locally, ואיך מעדכנים את השרת מהריפו (משיכה ידנית + הפעלה מחדש).
- הדגמה (GIF קצר, הקלטת מסך, או תמלול שיחה מודבק) שמראה את התרחיש המרכזי מקצה לקצה: זיהוי → הבהרה (אם צריך) → אימות → תשובה עם תיקון.

## ✅ צ'ק עצמי לפני הגשה

1. האם המערכת שלי חושפת תאריך/שעה/פרט כלשהו **לפני** אימות תעודת זהות מוצלח? (אם כן — זו בעיית אבטחה, לא פיצ'ר.)
2. אם יש שני לקוחות עם אותו שם פרטי, האם הבוט שואל הבהרה במקום לבחור אחד מהם?
3. יש הגבלת ניסיונות אימות שעובדת בפועל?
4. יש לי `DEPLOY.md` עם הוראות עדכון שרת אמיתיות מ-GitHub, לא רק "זה רץ אצלי מקומית"?
5. הרצתי בפועל את חמשת תרחישי הבדיקה למעלה — לא רק דמיינתי אותם?
6. בכל ENTITY כמו לקוח, פגישה, ליד וכו', חייבים להיות לפחות 5 שורות אמיתיות. רצוי לכל האופציות הקיימות במערכת
