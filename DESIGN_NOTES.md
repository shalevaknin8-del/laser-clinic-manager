# הערות עיצוב - למעצב/ת

מסמך זה משווה בין העיצוב המקורי (`desing/Clinic Suite.dc.html`) לבין
מה שהמערכת בפועל בנתה וצריכה. הממשק החי היום (`templates/`, `static/`)
מיישם את שפת העיצוב (צבעים, פונטים, כרטיסים) על **כל** המסכים, אבל
חלק גדול מהם **לא היו קיימים בעיצוב המקורי כלל** ותוכננו על ידי
Claude בהתאמה סגנונית - לא ע"י מעצב/ת אנושי/ת. המטרה של המסמך: לדעת
בדיוק אילו מסכים כדאי לעבור עליהם מחדש.

---

## 1. מסכים שלא היו בעיצוב המקורי בכלל

העיצוב שקיבלתי כלל 4 מסכים בלבד: **לוח בקרה, יומן, לקוחות (רשימה +
פרופיל), לידים**. כל מה שמתחת נבנה על ידי Claude מאפס, בהתאמה
סגנונית (צבעי זהב-שמנת, כרטיסים לבנים, RTL) אבל בלי ליווי עיצובי:

| מסך | קובץ | הערה |
|---|---|---|
| **התחברות** | `templates/login.html` | לא היה קיים בעיצוב כלל |
| **קטלוג וחבילות** (טבלה + טופס הוספה) | חלק מ-`static/app.js` (`renderPackages`) | בעיצוב המקורי זה היה פריט תפריט **לא פעיל** (בלי `onClick`) |
| **מכשירים וחדרים** | `renderResources` ב-`app.js` | גם זה היה פריט תפריט לא פעיל בעיצוב |
| **צוות וזמינות** (רשימת עובדות + שעות עבודה) | `renderStaff` | לא היה קיים כלל |
| **חשבוניות** | `renderInvoices` | לא היה קיים כלל |
| **יומן ביקורת** (audit log) | `renderAudit` | לא היה קיים כלל |
| **טופס תור חדש/עריכה** (מודל) | `openAppointmentModal` | בעיצוב היה רק כפתור "תור חדש" בלי טופס מאחוריו |
| **טופס לקוחה חדשה/עריכה** (מודל) | `openClientModal` | לא היה טופס בעיצוב, רק תצוגת פרופיל קיים |
| **העלאת הצהרת בריאות** | כפתור בפרופיל הלקוחה | בעיצוב היה שדה תצוגה בלבד ("הצהרת בריאות: חתומה"), בלי דרך להעלות מסמך |
| **טופס ליד חדש/עריכה + המרה ללקוחה** (מודל) | `openLeadModal` | הכרטיסים בעיצוב היו לתצוגה בלבד |
| **צ'אטבוט ללקוחות** (`/chat`) | `templates/chat.html` | לא הופיע בעיצוב בכלל - עיצבתי מחדש בפלטה זהה |

**המלצה:** אלה המועמדים הכי חשובים לעבור עליהם עם מעצב/ת - הם
מסכי עבודה יומיומיים (במיוחד טופס תור חדש, טופס לקוחה, וצ'אט
הלקוחות) שכרגע נראים "נקיים" אבל לא קיבלו שום מחשבת עיצוב ייעודית.

---

## 2. עודכן לאחר ביקורת: מה כן מומש במלואו

בסבב עדכון נוסף (אחרי שהושוותה התוצאה מול תצוגות אמיתיות של
העיצוב) הושלמו שני החלקים שתחילה פושטו:

- **היומן** - יש עכשיו **תצוגת שבוע אמיתית**: גריד עם עמודת שעות
  (09:00-19:00) ועמודה לכל יום (א'-ו'), עם בלוקים ממוקמים לפי
  שעה ומשך אמיתיים (כמו בעיצוב), וקליק על בלוק פותח עריכה. תצוגת
  "יום" נשארה כתצוגה נוספת, לא כתחליף.
- **צינור הלידים** - יש עכשיו **Drag & Drop אמיתי** (HTML5) בין
  העמודות: גוררים כרטיס לעמודה אחרת והסטטוס מתעדכן מיד מול ה-API.
- **לוח הבקרה** - המגמות (תורים/לידים/הכנסה מול התקופה הקודמת)
  **מחושבות אמיתיות מה-backend** (`api/dashboard_api.py`), לא
  מומצאות כמו בעיצוב המקור. אם אין עדיין נתון היסטורי (למשל
  אין הכנסה בחודש הקודם), מוצג 0 בכנות ולא דמו.
- **רשימת הלקוחות** - נוספו עמודת הכנסה מצטברת וחבילה פעילה עם
  פס התקדמות, מחושבים אמיתית מה-DB (`GET /api/clients`).

## 3. פישוטים שנשארו מכוונים

- **תצוגת חודש** ביומן (מסך גריד של 42 ימים) לא מומשה - רק
  יום/שבוע. תוספת עתידית קלה יחסית אם רוצים אותה.
- אין עדיין tooltip בריחוף עכבר על בלוקים ביומן - קליק פותח עריכה
  ישירות, שזה בפועל מהיר יותר לשימוש יומיומי.
- **פרופיל לקוחה**: שדות כמו "רמת לקוחה" (tier), "סוג עור
  (פיצפטריק)", "מכשיר מועדף" ו"רגישויות והערות" הופיעו בעיצוב
  אך **אינם קיימים במסד הנתונים היום**. הוסרו מהתצוגה עד שיוחלט
  אם להוסיף אותם כשדות אמיתיים.

---

## 4. מה שכן נשמר בנאמנות לעיצוב

- כל לוח הצבעים: רקע קרם (`#FAF9F6`), כרטיסים לבנים עם צל רך,
  זהב (`#C9AC79`/`#A8874F`/`#B99A64`) כצבע הדגשה, RTL מלא.
  הטוקנים מרוכזים ב-`static/style.css` תחת `:root`.
- הפונטים: Assistant לטקסט, Plus Jakarta Sans למספרים (Google
  Fonts, כמו בעיצוב המקורי).
- מבנה תפריט הצד, כותרת עליונה, וכרטיסי הסטטיסטיקה בלוח הבקרה -
  זהים למבנה בעיצוב (פינות עגולות 18px, אותו ריווח).
- מסך "פרופיל לקוחה" עם הטאבים (פרטים/היסטוריה/מסמכים) - אותו
  מבנה בדיוק, רק עם מקורות דאטה אמיתיים.

---

## 5. הרצה מקומית לצפייה

```bash
python3 app.py
```
ואז `http://127.0.0.1:5001` (התחברות) ו-`http://127.0.0.1:5001/chat`
(צ'אטבוט לקוחות).
